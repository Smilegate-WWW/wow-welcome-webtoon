server:
    address: localhost
    port: 8080
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/www?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: 1378
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL5InnoDBDialect
    generate-ddl: true
    show-sql: true
  servlet:    multipart:      enabled: true
      max-file-size: 50MB      max-request-size: 50MB
custom:
  path:
    upload-images : 'D:/upload/'
  